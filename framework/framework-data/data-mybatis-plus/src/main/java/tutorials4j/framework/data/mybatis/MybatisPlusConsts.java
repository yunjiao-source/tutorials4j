package tutorials4j.framework.data.mybatis;

/**
 * MyBatis Plus 框架内部常量定义。
 *
 * @author Yun Jiao
 */
public interface MybatisPlusConsts {
    int INTERCEPTOR_ORDER_DEFAULT = 1;
    int INTERCEPTOR_ORDER_TENANT = INTERCEPTOR_ORDER_DEFAULT + 1;
    int INTERCEPTOR_ORDER_PAGINATION = INTERCEPTOR_ORDER_TENANT + 1;
    int INTERCEPTOR_ORDER_OPTIMISTIC_LOCKER = INTERCEPTOR_ORDER_PAGINATION + 1;
    int INTERCEPTOR_ORDER_BLOCK_ATTACK = INTERCEPTOR_ORDER_OPTIMISTIC_LOCKER + 1;
}
