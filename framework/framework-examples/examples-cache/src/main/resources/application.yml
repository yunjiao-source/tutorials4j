spring:
  profiles:
#    active: cacheable
    active: lock
#    active: template
#    active: multi-level


  data:
    # Redis 配置
    redis:
      host: localhost # Redis服务器地址
      port: 6379 # Redis端口
      database: 2

management:
  endpoints:
    web:
      exposure:
        include: metrics,caches
  endpoint:
    caches:
      access: unrestricted

logging:
  level:
    tutorials4j: TRACE
#    org.springframework: debug