spring:
  profiles:
    active: redisson-lock
#    active: bitmap
#    active: template
#    active: multi-level
  #    active: cacheable

  data:
    # Redis 配置
    redis:
      host: localhost # Redis服务器地址
      port: 6379 # Redis端口

management:
  endpoints:
    web:
      exposure:
        include: metrics,caches
  endpoint:
    caches:
      access: unrestricted

logging:
  level:
    tutorials4j: DEBUG
#    org.springframework: debug