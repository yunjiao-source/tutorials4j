management:
  tracing:
    sampling:
      probability: 1.0   # 开发环境全采样；生产环境建议 0.1 左右
    propagation:
      type: w3c           # 推荐 W3C traceparent 格式，兼容性更好
  endpoints:
    web:
      exposure:
        include: "*"

