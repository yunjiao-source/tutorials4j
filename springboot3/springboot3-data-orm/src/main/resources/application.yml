spring:
  profiles:
#    active: mybatistenant
#    active: routingdatasourcejpa
    active: cursor


  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    tutorials4j: DEBUG
#    org.springframework: debug