# FAQ


## git使用

```text
使用 Git 时，关联正确的用户名和邮箱至关重要，因为每次提交（commit）都会永久记录这些信息，用于标识代码变更的作者
```

git config user.name "Yun Jiao"
git config user.email "1290308475@qq.com"

```text
添加其他远程仓库，如：github，gitcode
```
git remote add gitcode git@gitcode.com:yun-jiao/tutorials4j.git
git remote add github <你的GitHub仓库地址>

